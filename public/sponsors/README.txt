Add sponsor logo images here to display them automatically.

Expected filenames (matching `logo` paths in `src/lib/sponsors.ts`):
- hot-comet-games.png
- xyz-domains.png

If a logo file is missing, the sponsor's name text is shown instead.
